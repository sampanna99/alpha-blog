<h1 align="center">welcome to <%= @chalaune.username %> </h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @chalaune, size: 150 %>
    </div>
  </div>
<h4 align="center"> <%= @chalaune.username %> 's article</h4>
<%= render 'articles/article', obj: @chalaune.articles %>